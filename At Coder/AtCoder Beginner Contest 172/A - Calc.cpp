#include<bits/stdc++.h>
#define pi acose(-1)
#define rep(i,x) for(int i=1; i<=(x); i++)
typedef long long ll;
using namespace std;

int main()
{
    int a, n,sum;
    cin>>a;
    sum = a+ pow(a,2) + pow(a,3);
    cout<<sum<<endl;

    return 0;
}
